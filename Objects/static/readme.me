公有静态成员：在JavaScript中并没有特殊语法来表示静态成员。但是可以通过使用构造函数并且向其添加属性这种方式，从而获得与“类式”语言相同的语法

私有静态成员：就私有静态成员而言，指的是成员具有如下属性：
1、以同一个构造函数创建的所有对象共享该成员
2、构造函数外部不可访问该成员

静态属性使用会带来很多便利。它们可以包含非实例相关的方法和数据，并且不会为每个实例重新创建静态属性。
当涉及单体模式时，可以看到一个静态属性以实现类似类的单体构造函数的例子